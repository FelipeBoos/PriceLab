#!/usr/bin/env sh

echo "Rodando testes do backend antes do commit (husky)..."

cd backend || exit 1

./mvnw -B test
RESULT=$?

if [ $RESULT -ne 0 ]; then
  echo "Os testes falharam. Commit cancelado."
  exit 1
fi

echo "Testes passaram. Prosseguindo com o commit."
exit 0